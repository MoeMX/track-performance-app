# TrackPerf — Track Runner Performance App
React + Vite | Firebase Auth + Firestore | Cloudflare Pages

## Quick Start
1. Copy `frontend/.env.example` to `frontend/.env.local` and fill in Firebase values
2. `cd frontend && npm install && npm run dev`

## Deploy
Push to `main` branch — Cloudflare Pages auto-deploys.
